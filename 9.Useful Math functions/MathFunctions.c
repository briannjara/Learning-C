#include <stdio.h>
#include <math.h>  //to include math functions

int main (){

    double A = sqrt(9);   //Square root
    double B = pow(2, 4); //Power 2^4
    int C = round(3.14);  //Rounding off
    int D = ceil(3.14);   //Rounding down
    int E = floor(3.99);  //Rounding up
    double F = fabs(-100);  //Absolute value
    double G = log(3);    //logarithms
    double H = sin(45);
    double I = cos(45);
    double J = tan(45);

    printf("\n%d", C);

    return 0;
}